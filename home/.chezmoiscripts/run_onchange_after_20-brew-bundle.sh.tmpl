#!/bin/sh
# Brewfile hash: {{ include "dot_Brewfile" | sha256sum }}
# Reruns when ~/.Brewfile changes. Installs missing entries only. Never upgrades, never uninstalls.
set -eu
eval "$(/opt/homebrew/bin/brew shellenv)"
brew bundle --global --no-upgrade
